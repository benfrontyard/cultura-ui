/** Home dashboard — workspace/index.js owns element-gated inits; hook page-only logic here. */
export function initPage() {
  /* initDashboardGreeting runs from workspace when #chDashboardGreeting exists */
}
