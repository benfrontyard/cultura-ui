/**
 * Chat exchange orchestration — composer submit, typing delay, dock integration.
 * Full implementation currently in workspace/index.js (`dispatchChatExchange`, `initChatComposer`).
 */
export { chatStore, createChatMessage, resetChatStore } from "./chat-store.js";
