/**
 * Chat DOM rendering — bubbles, markdown, streaming.
 * Full implementation currently in workspace/index.js; migrate functions here incrementally.
 */
export { buildChatAuthorHtml, buildUserBubbleHtml, buildTypingIndicatorHtml } from "./chat-templates.js";
